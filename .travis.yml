## reference: https://docs.travis-ci.com/user/languages/r/

language: r
r: bioc-devel

cache: packages

r_packages:
  - statmod

r_github_packages:
  - r-lib/covr
  - pkimes/upbmData
  - pkimes/upbmAux

after_success:
  - Rscript -e 'library(covr); codecov()'
